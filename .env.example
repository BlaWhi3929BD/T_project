# Copy this file to .env and replace every change-me value before starting Compose.
POSTGRES_DB=trades_db
POSTGRES_USER=trades_user
POSTGRES_PASSWORD=change-me

# Keep these credentials aligned with the PostgreSQL settings above.
DATABASE_URL=postgresql://trades_user:change-me@postgres:5432/trades_db
